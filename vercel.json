{
    "version": 2,
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/node"
      }
    ],
    "routes": [
      { "src": "/(.*)", "dest": "/" }
    ],
    "env": {
      "DATABASE_URL": "@your_database_url"
    },
    "build": {
      "scripts": {
        "build": "prisma generate"
      }
    }
  }
  